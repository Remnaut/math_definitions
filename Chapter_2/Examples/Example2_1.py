"""Example 2.1------------------------------------------A Ball Dropped From a Tower------------------------------------------Function: The program finds the height of      a ball after a specific time.Input: The user inputs the height of the      tower and the time interval passed.Output: The program outputs the height of      ball based on user inputs."""# User inputs the heighth = float(raw_input('Enter the height of the tower: '))# User inputs the time intervalt = float(raw_input('Enter the time interval: '))# Distance traveled due to gravityg = 9.81 #meters per second**2s = (9.81 * t ** 2)/2 #meters# Print the height based on user parametersprint 'The height of the ball is', h - s, 'meters.'